#include "main.h"

/**
 * _islower - Checks for lowercase character
 * @c: a character argument
 * Return: returns 0 and 1 depending the context
 */

int _islower(int c)

{
	return (c >= 'a' && c <= 'z');
}
